/**
 * Topic: Operators and Comparisons
 * 
 * Instructions:
 * 1. Read the comment above each section carefully.
 * 2. Write your code in the empty space provided.
 * 3. Focus on data types (Number vs String).
 * 4. Run this file with 'node src/operators/exercise/02-challenges.js'
 */

// --- EXERCISE 1: Basic Addition ---
// Create two constants: 'price' (100) and 'tax' (20).
// Create a let variable 'total' that adds them together.
// Print 'total'.


// --- EXERCISE 2: Subtraction (Change) ---
// Create a const 'cash' (50) and 'cost' (35).
// Create a let variable 'change' that subtracts cost from cash.
// Print 'change'.


// --- EXERCISE 3: Multiplication (Area) ---
// Create a const 'width' (10) and 'height' (5).
// Create a let variable 'area' that multiplies them.
// Print 'area'.


// --- EXERCISE 4: Division (Average) ---
// Create a const 'score1' (80) and 'score2' (90).
// Create a let variable 'average' that adds them and divides by 2.
// Print 'average'.


// --- EXERCISE 5: Modulus (Even or Odd) ---
// Create a const 'number' (7).
// Use the modulus operator (%) to check if it is even.
// Create a boolean variable 'isEven' that is true if remainder is 0.
// Print 'isEven'.


// --- EXERCISE 6: Modulus (Remainder) ---
// Create a const 'items' (10) and 'boxSize' (3).
// Create a let variable 'remainder' that shows items left over after filling boxes.
// Print 'remainder'.


// --- EXERCISE 7: The Concatenation Trap ---
// Create a const 'scoreString' ("100") and 'bonusNumber' (50).
// Create a let variable 'totalScore' that adds them.
// Print 'totalScore' and observe the result (String vs Number).
// Print the typeof 'totalScore'.


// --- EXERCISE 8: Greater Than ---
// Create a const 'age' (20) and 'legalAge' (18).
// Create a boolean 'isAdult' that checks if age is greater than legalAge.
// Print 'isAdult'.


// --- EXERCISE 9: Less Than or Equal ---
// Create a const 'speed' (100) and 'limit' (100).
// Create a boolean 'isSafe' that checks if speed is less than or equal to limit.
// Print 'isSafe'.


// --- EXERCISE 10: Strict Equality (Number vs String) ---
// Create a const 'inputId' ("101") and 'databaseId' (101).
// Create a boolean 'isMatch' using STRICT equality (===).
// Print 'isMatch' (Expect false because types differ).


// --- EXERCISE 11: Strict Inequality ---
// Create a const 'status' ("active") and 'bannedStatus' ("banned").
// Create a boolean 'isAllowed' that checks if status is NOT STRICTLY equal to bannedStatus.
// Print 'isAllowed'.


// --- EXERCISE 12: Loose Equality Warning ---
// Do NOT write code here. Just read:
// If you used == in Exercise 10, the result would be true.
// This is why we always use ===.
// Write a comment below confirming you understand why === is safer.


// --- EXERCISE 13: Access Control Logic ---
// Create a const 'balance' (50) and 'itemCost' (100).
// Create a boolean 'canBuy' that checks if balance is greater than or equal to itemCost.
// Print 'canBuy'.


// --- EXERCISE 14: Discount Calculation ---
// Create a let 'finalPrice' (100).
// If a user is a member (const 'isMember' = true), subtract 10 from finalPrice.
// Use an if statement and strict equality (===).
// Print 'finalPrice'.


// --- EXERCISE 15: Combined Operators ---
// Create a const 'baseSalary' (1000).
// Create a const 'bonus' (200).
// Create a const 'taxRate' (0.1).
// Calculate 'totalSalary' = (baseSalary + bonus) - ((baseSalary + bonus) * taxRate).
// Print 'totalSalary'.